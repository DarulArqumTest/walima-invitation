{
  "name": "walima-invitation",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Walima wedding invitation — static frontend + Vercel serverless API + Vercel Postgres",
  "engines": { "node": ">=18" },
  "dependencies": {
    "@vercel/postgres": "^0.9.0"
  }
}
